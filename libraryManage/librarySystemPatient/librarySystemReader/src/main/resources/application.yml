eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
spring:
  application:
    name: library-system-reader
server:
  port: 8081
feign:
  client:
    config:
      default:
        connectTimeout: 5000
        readTimeout: 3000